{
  "name": "releasedate",
  "version": "1.0.0",
  "description": "Simple release dates indicator that shows in the status bar the current date with the format [YEAR+YEAR_OR_WEEK].",
  "author": "David Cordero",
  "license": "GPL-3.0",
  "main": "main.js",
  "scripts": {
    "lint": "eslint . --ext .js",
    "start": "electron .",
    "build": "electron-packager . ReleaseDate --platform=darwin --icon='assets/icons/mac/icon.icns' --arch=x64 --app-version=1.0.0 --overwrite --out build"
  },
  "dependencies": {
    "moment": "^2.22.2"
  },
  "devDependencies": {
    "electron": "^3.0.4",
    "electron-packager": "^12.2.0",
    "eslint": "^5.6.1"
  }
}
